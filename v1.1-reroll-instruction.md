# 給大哥(Claude Code)的指令 — v1.1 #1:每日重抽懸賞(Rogue-lite 機制)

> 來自 chat(二哥)。這是 v1.0 上線後第一個反饋項。
> 機制與 schema 欄位在 v3.4 規格就設計過了(users 表已有 `reroll_used` / `reroll_date`),但 UI 和邏輯還沒做。這次補完整。

## 概念
懸賞板加一個「重新張貼委託」按鈕,讓玩家重抽當前的三張懸賞。但**每日有限次數,且次數隨等級提升解鎖更多**——Rogue-lite 風格的「每日限定嘗試 + 隨進度開放更多」。

## 每日重抽次數(依即時等級)
```
見習獵人   1 次/日
青銅獵人   2 次/日
白銀獵人   3 次/日
黃金獵人   4 次/日
美食獵人   5 次/日
傳說級獵人 6 次/日
```
- 等級由 hunts 的 EXP 即時算(同現有邏輯),不另存
- 每日上限 = 依當前等級查上表

## 後端邏輯(schema 欄位已存在)
- `users.reroll_date`:重抽額度的基準日;`users.reroll_used`:當日已用次數
- 玩家按重抽時:
  1. 比對 `reroll_date` 是否為今日(Asia/Taipei)。不是今日 → 歸零 `reroll_used`、更新 `reroll_date` 為今日
  2. 算當前等級的每日上限。若 `reroll_used >= 上限` → 拒絕,回友善提示
  3. 否則 → `reroll_used += 1`,重新抽三張懸賞(用新種子,讓結果不同),回新懸賞
- **重抽的種子**:現有懸賞固定邏輯是 `user_id + date + origin_key`。重抽要產生不同結果,種子加入 `reroll_used`(如 `user_id + date + origin_key + reroll_used`),這樣每次重抽都不同、但同一次重抽結果穩定
- **重抽一樣要套既有排除規則**:7 天內已討伐的店不抽出(這是蚵仔威早期回報的,別退化)

## UI(懸賞板)
- 懸賞板加「⟳ 重新張貼委託」按鈕(羊皮紙/木質風,別做成突兀的網頁按鈕)
- 顯示剩餘次數,如「今日可重張貼 2/3」
- 次數用完 → 按鈕變灰 + 友善提示:「今日的委託已看遍,獵人。明日公會將張貼新的委託。」
- (可選)按鈕旁小字提示「升級可解鎖更多次數」,讓玩家知道升級有這個好處 → 強化升級動機

## 驗收
- 重抽按鈕出現在懸賞板,點了能換一批懸賞
- 每日次數依等級正確(見習 1 次、白銀 3 次…)
- 次數用完按鈕停用+提示;跨日(Asia/Taipei)歸零
- 重抽結果與原本不同,但 7 天內已討伐的店仍不出現
- 重抽不消耗、不影響 EXP/討伐紀錄(純換懸賞,不是討伐)

## 不要動
- 討伐流程、EXP 公式、四態邏輯、schema → 別重構
- 重抽只換「懸賞板顯示的三張」,不碰已接受/已討伐的紀錄
