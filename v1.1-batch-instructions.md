# 給大哥(Claude Code)的指令 — v1.1 批次(硬推廣前)

> 來自 chat(二哥)。上線後第一批改進,硬推廣前該補的東西。
> ⚠ **任務 2(資安)請走 Opus 4.8**(富毅工作流規則:資安審計直接用 Opus,Fable 5 會降級故不用)。
> 三件事依序做,每件 checkpoint。

---

## 任務 1 — 每日重抽懸賞(Rogue-lite 機制)

詳見 repo 裡 `v1.1-reroll-instruction.md`(已另附完整規格)。重點摘要:
- 懸賞板加「⟳ 重新張貼委託」按鈕,每日限次,次數隨等級解鎖更多(見習1→傳說6)
- schema 欄位 `users.reroll_used`/`reroll_date` 已存在,補 UI + 邏輯
- 種子加 `reroll_used` 確保重抽結果不同;7天內已討伐店仍排除;跨日(Asia/Taipei)歸零

---

## 任務 2 — 基礎資安加固(走 Opus 4.8)⭐

> 範圍:擋掉「硬推廣後會被腳本小子/好奇玩家/有心人士搞」的常見洞。**目標是務實加固,不是金融級審計。** 重點是「容易被搞 + 被搞了最痛」的幾個面向。

請審查並加固以下項目,逐一回報現況(已安全/已修復/有風險):

**2a. 防止偽造請求灌 EXP / 改紀錄(最重要)**
- 討伐完成 `POST /bounty/<id>/complete` 等寫入端點:確認有驗證登入 session、且**只能操作自己的資料**(不能帶別人的 user_id 改別人紀錄)
- 確認玩家不能繞過「接懸賞 → 討伐」流程,直接打 complete API 灌 EXP(要驗證該 bounty 屬於該 user、狀態合法、未完成過)
- 重抽、討伐等狀態變更端點都要做同樣的「歸屬驗證」

**2b. XSS 防護(短評輸入)**
- 玩家輸入的 `review_text`(短評)在圖鑑/任何地方顯示時,必須**跳脫 HTML**(Jinja2 預設 autoescape 有開就好,但確認沒有用 `|safe` 把它放出去)
- 任何使用者可輸入的文字(隊名/短評/自評)顯示時都要跳脫

**2c. API 額度 / 速率保護**
- 確認 Google Places API 呼叫**只在後端**、key 不暴露到前端原始碼
- 加基礎速率限制(rate limiting):防止單一使用者/IP 瘋狂打 API 端點(尤其會觸發 Places API 的搜尋端點),避免額度被惡意燒爆或 Supabase 被灌爆
- 確認網格快取邏輯沒有可被繞過、強制每次都打 API 的路徑

**2d. 機密與設定**
- 確認所有 key(Supabase service_role、Google、LINE secret、Flask secret)都在環境變數,沒有任何一個 commit 進 git、沒有印在前端
- 確認 `FLASK_ENV=production`、debug 模式關閉(production 開 debug 會洩漏堆疊資訊)
- session cookie 設定:`Secure`(HTTPS)、`HttpOnly`、`SameSite`

**2e. LINE OAuth**
- 確認 callback 的 `state` 參數有正確驗證(防 CSRF),且用後即棄
- 確認驗證失敗的各種情況都安全導回登入,不會半登入狀態

**回報格式**:每項標「✅已安全 / 🔧已修復 / ⚠️有風險待討論」,有風險的說明影響,別默默跳過(Rule 12)。

---

## 任務 3 — 修 LINE 內建瀏覽器定位問題 ⭐

**問題**:LINE App 內建瀏覽器(in-app browser)常常拿不到定位權限,只有原生瀏覽器(Safari/Chrome)能抓到。但玩家多半從 LINE 點連結進來、停在 LINE 內建瀏覽器,於是按「搜索此地」(開拓模式)定位失敗、卡住,以為壞了。這是「最多人會走到的路上的地雷」。

**做法**(優先 b + c,把既有的遠征模式當救生艇):
- **a. 偵測 LINE 內建瀏覽器**:可由 User-Agent 偵測(含 "Line" 字樣),提示「在 LINE 中無法使用定位,請點右上角『⋯』用 Safari/Chrome 開啟」
- **b. 定位失敗優雅降級**:不論在哪個瀏覽器,只要定位抓不到/被拒,**不要讓玩家卡死**——顯示友善提示並**引導到遠征模式**(遠征選區不需要定位,一樣能討伐)。例:「定位抓不到?沒關係,改用『遠征規劃』選個行政區,一樣能接懸賞 →」
- **c. 把遠征入口在定位失敗畫面做明顯**,讓玩家一鍵轉過去

**核心精神**:開拓模式靠定位,但遠征模式天生不需要定位。定位失敗時,把玩家導向遠征,bug 就變成「引導到另一條路」,體驗救回來。

**驗收**:在 LINE 內建瀏覽器開啟、定位失敗時,玩家有清楚的下一步(換瀏覽器 或 用遠征),不會卡在白屏/轉圈。

---

## 不要動
- 已正確的後端邏輯、schema、刻意設計(C=60、二刷×0.3、隱藏名店×2、等級不存欄位)
- 四態邏輯、種子抽取、討伐流程

## 完成後
- 更新 Session Snapshot
- 資安項目附逐項回報清單(✅/🔧/⚠️)
