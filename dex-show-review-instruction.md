# 給大哥(Claude Code)的小指令 — 圖鑑顯示討伐短評

> 來自 chat(二哥)。把「已經存好但沒顯示」的資料秀出來,不是新功能。

## 問題
討伐完成時,使用者填了自評星數(`player_rating`)和短評(`review_text`),這兩個都已寫進 `hunts` 表。但圖鑑(`/dex`)目前只顯示店名、分級、星數、日期,**沒有把使用者自己寫的短評 `review_text` 顯示出來**。

那段短評是使用者的「狩獵日記」,寫了卻看不到很可惜。圖鑑顯示它,就從「分數列表」變成「個人美食回憶錄」。

## 做法
在圖鑑的每筆討伐紀錄(收藏卡)上,**若該筆 `hunts.review_text` 不為空,就顯示出來**——放在星數/日期下方,做成「手寫筆記」感(斜體、引號、或便條紙質感),呼應 RPG 日記調性。

- 資料已在 `hunts.review_text`,直接 select 出來顯示即可,不用改 schema
- `review_text` 為空(使用者沒寫)→ 該筆就不顯示這塊,不要留空白框
- 短評僅本人可見(本來就是,維持)

## 範例呈現(參考,風格自訂)
```
摩斯漢堡・台南北安店   [B]
★★★☆☆  +30 EXP  2026-06-15
「這家的米漢堡意外地耐吃,薯條普通。」  ← review_text
```

## 驗收
- 有寫短評的討伐紀錄,圖鑑看得到那段短評
- 沒寫短評的紀錄不顯示空框
- 視覺對齊木質/羊皮紙風,別做成白底純文字
